<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>編輯文章｜Goblin Blog</title>
  <style>
    body { font-family: sans-serif; padding: 2em; }
    input, textarea { width: 100%; margin-bottom: 1em; padding: 0.5em; }
    button { padding: 0.5em 1em; }
  </style>
</head>
<body>
  <h1>✏️ 編輯文章</h1>
  <form method="POST" action="/dash/edit/<%= post.id %>">
    <label>標題：</label>
    <input name="title" value="<%= post.title %>" required>

    <label>內容：</label>
    <textarea name="content" rows="10" required><%= post.content %></textarea>

    <button type="submit">💾 儲存修改</button>
  </form>
  <p><a href="/dash">← 回到 Dash</a></p>
</body>
</html>
